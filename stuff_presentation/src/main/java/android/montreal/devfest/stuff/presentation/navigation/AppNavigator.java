package android.montreal.devfest.stuff.presentation.navigation;

import android.content.Context;

public interface AppNavigator {
    void showViewMore(Context context);

    void showWebsite(Context context);
}
