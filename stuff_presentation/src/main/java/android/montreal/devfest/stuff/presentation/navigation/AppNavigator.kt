package android.montreal.devfest.stuff.presentation.navigation

import android.content.Context

interface AppNavigator {
    fun showViewMore(context: Context)

    fun showWebsite(context: Context)
}
